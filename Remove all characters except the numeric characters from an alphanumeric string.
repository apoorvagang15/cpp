class Solution{
public:	

	string removeCharacters(string S) 
	{
	    
	    string r;
	    for(int i=0;i<S.size();i++){
	        if(isdigit(S[i])){
	            r+=S[i];
	        }
	    }
	    return r;
	}
};
