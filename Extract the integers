vector<string> extractIntegerWords(string s)
{
    vector<string>t;
    int i=0;
    while(i<s.size()){
        string r;
        while(s[i]>='0' && s[i]<='9'){
            r+=s[i];
            i++;
        }
        if(r.size()>0)
            t.push_back(r);
        i++;
        
    }
    return t;
}
